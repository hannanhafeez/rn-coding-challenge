const baseUrl = process.env.NODE_ENV === 'development' ? "https://dolphin-app-jce8z.ondigitalocean.app" : "https://dolphin-app-jce8z.ondigitalocean.app";

const intermediary = "/api/user";

export const API_LINKS = {

};